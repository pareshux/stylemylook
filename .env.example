# Copy to .env.local and fill in values.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required for POST /api/waitlist (Dashboard → Settings → API → service_role secret)
SUPABASE_SERVICE_ROLE_KEY=
